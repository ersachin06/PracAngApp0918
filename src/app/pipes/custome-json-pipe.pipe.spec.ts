import { CustomeJsonPipePipe } from './custome-json-pipe.pipe';

describe('CustomeJsonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeJsonPipePipe();
    expect(pipe).toBeTruthy();
  });
});
